# Core dependency source file for requirements.lock
# Install from the lockfile instead: python -m pip install --require-hashes -r requirements.lock
# Regenerate the lock with: uv pip compile --generate-hashes requirements.txt -o requirements.lock
PyYAML==6.0.3

# Test/dev tooling — runs the repo regression tests under tests/
pytest>=9.1.1

# Optional — only needed by specific skills
# Keep these pinned if you activate them and regenerate requirements.lock.

# skills/gpt-imagen (OpenAI image generation)
# openai>=1.0.0
# Pillow>=10.0.0

# skills/gemini-imagen (Google Gemini image generation)
# google-genai>=1.0.0
# Pillow>=10.0.0
