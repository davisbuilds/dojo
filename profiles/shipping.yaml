# Reviewed data. Changing `members` changes profile identity — see profiles/README.md.
name: shipping
kind: overlay
description: >-
  Deploy to Vercel and inspect deployment build/runtime logs. Ordinary GitHub
  publication uses the agent's existing tools and repository guidance; it does
  not need a catalog skill.
members:
  - vercel-deploy        # SC-02 anchor: publish the deployment
  - vercel-preview-logs  # SC-02 anchor: inspect whether the deployment worked
