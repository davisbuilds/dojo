# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editors and IDEs
.vscode/
.idea/
*.swp
*.swo
.netrwhist
Session.vim

# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.local.json

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.eslintcache
.pnpm-debug.log*
.pnpm-store/

# Python
__pycache__/
.pytest_cache/
*.py[cod]
*$py.class
.venv/
venv/
ENV/

# Logs and temporary files
*.log
tmp/
temp/

# Skill build artifacts
*.skill

# Misc
anthropic/

# Docs
docs/downloads/
docs/archive/
docs/sessions/

# Codex / agents harnesses — mirror the .claude convention below:
# settings.json is tracked; the skills symlink is generated per clone
# by scripts/gen_harness_adapters.py. (Local overrides via *.local.json.)
.agent/skills
.agents/skills

# Claude Code — ignore user-specific subdirs; settings.json is tracked.
# `.claude/skills` is a generated symlink -> ../skills (regenerated per clone
# by scripts/gen_harness_adapters.py), so ignore it as a file or directory.
.claude/skills
.claude/commands
.claude/todos/
.claude/projects/

# skill-standardizer backup snapshots
.skill-standardizer/backups/
# per-checkout debounce state for the SessionStart drift notice
.skill-standardizer/drift-state.json
skills-lock.json

# Playwright CLI runtime snapshot dumps (written under the CWD wherever the CLI
# runs — repo root, the skill dir, or output/playwright/<label>/ — never part of
# a skill). Repo-wide so no run leaves stray snapshots tracked.
**/.playwright-cli/
