# Adapter Examples

This directory is intentionally lightweight.

Use it for harness-local examples such as:

- copying approved `promote-memory-note` proposals into a memory file
- reminding the agent to compact the store at session end
- scheduling periodic compaction jobs

Keep the canonical behavior in `SKILL.md` and `references/`. Do not make these examples required for the core workflow.
