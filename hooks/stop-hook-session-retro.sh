#!/usr/bin/env bash
# Non-blocking reminder; a session ending does not itself require a retro.
# Prints to stderr and always exits 0.

echo "Reminder: if a consequential learning is still missing from the canonical project docs and the update is authorized, capture it there; /retro can help. Skip if already captured or nothing durable is missing." >&2
exit 0
